spring:
  application:
    name: game-engine
  mvc:
    problemdetails:
      enabled: true

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true
# Root Lombok configuration for all modules.
config.stopBubbling = true

# Let @RequiredArgsConstructor / @AllArgsConstructor copy Spring's @Value onto
# the generated constructor parameter, so configuration properties can be
# constructor-injected into a final field instead of field-injected.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Value
